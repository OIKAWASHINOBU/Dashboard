# 実践会 統合司令塔ダッシュボード

このリポジトリは単一ファイル `index.html` で動くダッシュボードです。
UTAGE、Google Drive(スプレッドシート)、Zoho CRM のMCPツールを `window.cowork.callMcpTool` で直接呼び、
KPIとアクションボードを描画します。

## 編集の基本

- `index.html` は約530KBあります。該当箇所だけを部分編集し、ファイル全体を書き直しません。
- 依頼された範囲だけを変更します。周辺で見つけた別の不具合や整理は、変更せずに最後に報告します。
- 変更後はブラウザで開き、対象タブの表示を確認してから完了とします。

## 数値の扱い(最優先)

このダッシュボードの価値は数字の正確さです。以下を必ず守ります。

### 1. 出典を確認してから使う

- 数値は必ずツール出力(UTAGE、シート、Zoho)か `index.html` 内の確定値から取ります。
  記憶や推測で数字を埋めません。
- 取得できなかった値は「未取得」「未確認」と表示し、それらしい数字で代用しません。
- シート未入力時は 7/13 スナップショット(`asOf:"2026-07-13"`)へフォールバックします。
  フォールバック値を使った場合は、表示と報告の両方で「7/13時点」と明記します。

### 2. 定義を混同しない

- CPAは2種類あり、分母が違うため一致しません。
  - 仮エントリーCPA = 広告費 ÷ 広告経由仮エントリー
  - 入会CPA = 広告費 ÷ 広告経由入会(決済完了のみ)
- 広告費は「実数」欄の確定分を使います。予定額や概算と混ぜません。
- 金額は税込・税抜のどちらかを明示します。混在する場合は分けて表示します。
- 「申込」「仮エントリー」「入会(決済完了)」は別の段階です。集計時にまとめません。

### 3. 期間と母数をそろえる

- 集計期間(開始日・終了日)、基準日(asOf)、対象シナリオ・対象講演会を先に固定してから数えます。
- 率(開封率、クリック率、CVR)は分子と分母を明示し、母数が違う数字同士を比べません。
- Zohoのクエリは日付条件(例: `field1 >= '2026-05-28T00:00:00+09:00'`)と `limit` を持ちます。
  `limit` に達した場合は件数が切れている可能性があるため、そのまま合計に使わず報告します。

### 4. 計算は検算する

- 合計、差分、率は、元データから再計算して一致を確認します。
- 単位(円、件、%)、桁、丸め方(小数点以下の桁数)を表示前にそろえます。
- 結果を左右する計算は、別の方法(別の列、別のツール出力)でも一度確かめます。

### 5. 報告の書き方

- 「確認した数値」「そこから導いた判断」「未確認の項目」を分けて書きます。
- 実行していない取得、照合、保存を実行済みとして書きません。
- 数値を更新したときは、変更前後の値と出典(どのツール・どのシート・どのセル)を残します。

## データ源の対応表

| 用途 | ツール | 備考 |
| --- | --- | --- |
| ファネル統計 | UTAGE `funnel_stats_get` / `funnel_stats_daily` | 講演会・エバー |
| 読者・シナリオ | UTAGE `message_reader_list` / `message_scenario_stats_list` / `message_list` | メルマガ分析 |
| 月次施策の数値 | Google Drive `read_file_content` | シート「実践会_7月施策」数値計測タブ |
| 申込・入会 | Zoho CRM `executeCOQLQuery` | CustomModule7 / CustomModule8 |

## 依頼の書き方(利用者向けメモ)

数値に関わる依頼には次の3点を一行添えると、照合できる成果物になります。

- 基準日(例: 2026-09-09時点)
- 対象(例: 7/22セミナー、講演会ファネルA)
- 照合先(例: シート「実践会_7月施策」の数値計測タブ、UTAGEのシナリオ統計)
